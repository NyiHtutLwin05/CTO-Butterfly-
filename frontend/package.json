{
  "name": "butterfly-frontend",
  "version": "1.0.0",
  "description": "Frontend for Butterfly Observation Platform",
  "scripts": {
    "start": "live-server frontend --port=8080",
    "dev": "live-server frontend --port=8080"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
